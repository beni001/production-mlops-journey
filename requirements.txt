# ================================================================
# TRAINING DEPENDENCIES — pinned exact versions
# CACHE Principle defense: every version locked = reproducible model
#
# Upgrade path:
#   - Moving to GPU cloud instance? Add: cupy-cuda11x==12.3.0
#   - Moving to AWS S3 for DVC?    Add: dvc-s3==3.1.0
#   - Moving to GCS for DVC?       Add: dvc-gs==3.0.0
# ================================================================

# ── Core ML Stack ─────────────────────────────────────────────
# xgboost: gradient boosting — primary model for rideshare demand
# Why 2.0.3: stable GPU/CPU API, works with sklearn pipeline interface
scikit-learn==1.3.2
xgboost==2.0.3
pandas==2.1.4
numpy==1.26.2
scipy==1.11.4

# ── Model serialization ────────────────────────────────────────
# joblib: saves model.pkl — the artifact that crosses the
# training/inference boundary via MODEL_PATH
joblib==1.3.2

# ── Data versioning (Day 4) ────────────────────────────────────
# dvc: versions datasets the way git versions code
# Local remote only (KVM infra) — no dvc-s3 needed yet
dvc==3.38.1

# ── Experiment tracking (Day 5+) ──────────────────────────────
# mlflow: logs metrics, params, artifacts per training run
# Lets you compare "did retraining on Day 30 improve over Day 5?"
mlflow==2.10.0

# ── Feature engineering ────────────────────────────────────────
# feature-engine: same transformations in training AND inference
# Prevents training-serving skew (Day 8 feature store concept)
feature-engine==1.6.2

# ── CLI + config ───────────────────────────────────────────────
# click: powers the --data-path --model-output flags in train.py
# python-dotenv: loads .env files (never commit .env to git)
click==8.1.7
python-dotenv==1.0.1
pyarrow==15.0.0
